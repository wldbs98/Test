<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.test.dao.CarRegisterDAO">
	<select id="loginOk" resultType="com.study.test.dto.CarRegisterDTO">
 		select id, name from userInformation where id=#{param1} and password=#{param2}
 	</select>
</mapper>